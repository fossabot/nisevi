/* Replace html on welcome-index view. */
// clean form fields after contact info was sent
$("#contact_name").val("")
$("#contact_email").val("")
$("#contact_message").val("")

// reset reCAPTCHA after comment was created
grecaptcha.reset();
